csp {
  default-src = "default-src 'self'" ${?ASSETS_URL}
  img-src = "img-src 'self'" ${?ASSETS_URL} "https://platform.slack-edge.com"
  style-src = "style-src 'self'" ${?ASSETS_URL} "https://fonts.googleapis.com"
  font-src = "font-src 'self'" ${?ASSETS_URL} "https://fonts.googleapis.com" "https://fonts.gstatic.com"
  object-src = "object-src 'none'"
  frame-ancestors = "frame-ancestors 'none'"
}

play {
  filters {
    headers {
      contentSecurityPolicy = ${csp.default-src}; ${csp.img-src}; ${csp.style-src}; ${csp.font-src}; ${csp.object-src}; ${csp.frame-ancestors};
    }
  }
}
